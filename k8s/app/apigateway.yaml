apiVersion: apps/v1
kind: Deployment
metadata:
  name: apigateway
  namespace: app
spec:
  revisionHistoryLimit: 3
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: apigateway
  template:
    metadata:
      labels:
        app: apigateway
    spec:
      containers:
        - name: apigateway
          image: tangnhatdang/ecommerce-kltn-apigateway:ee4e905
          ports:
            - containerPort: 9090
          env:
            - name: PORT
              value: "9090"
            - name: AUTH_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: AUTH_SERVICE_ADDR
            - name: PRODUCT_CATALOG_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: PRODUCT_CATALOG_SERVICE_ADDR
            - name: CART_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: CART_SERVICE_ADDR
            - name: CHECKOUT_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: CHECKOUT_SERVICE_ADDR
            - name: PAYMENT_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: PAYMENT_SERVICE_ADDR
            - name: SHIPPING_SERVICE_ADDR
              valueFrom:
                configMapKeyRef:
                  name: common-config
                  key: SHIPPING_SERVICE_ADDR
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: 90
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9090
            initialDelaySeconds: 90
            periodSeconds: 10
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: app
  labels:
    app: spring-service
spec:
  selector:
    app: apigateway
  ports:
    - name: http
      port: 9090
      targetPort: 9090
  type: ClusterIP
